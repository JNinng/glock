# glock

后端无关的分布式锁库。核心模块定义锁的语义契约，各后端（Redis、Postgres）以独立 module 按需引用实现。

## Language

### 持有关系

**Key（锁键）**:
被互斥保护的资源名字。同一 Key 在任一时刻最多被一个写持有，或被多个读持有。
_Avoid_: 锁名、资源 ID

**Hold（持有）**:
一次成功获取到释放（Release）或丢失（Lost）之间的持有关系。句柄（handle）是 Hold 在代码中的表现形式。
_Avoid_: 会话、连接

**Owner Token（身份令牌）**:
持有者的稳定身份标识，默认自动生成，可由使用者显式指定。可重入计数与防误删都以它为判据。
_Avoid_: token（与 Fencing Token 混淆）、holder ID

**Lost（丢失）**:
租约到期或续期失败后，本地 Hold 不再背书互斥的状态。丢失后的任何释放操作必须拒绝，不得误删他人锁。
_Avoid_: 过期（那是 Lease 的状态，不是 Hold 的）

### 时间语义

**Lease（租约）**:
锁在后端存活的时限，到期由后端自动释放。所有锁必须有界租约，不存在无限持有。
_Avoid_: TTL、超时时间

**Watchdog（自动续期）**:
持有期间周期性延长租约的后台活动。进程死亡后租约自然到期，锁自动让出。
_Avoid_: 心跳、保活

### 安全语义

**防误删（Safe Release）**:
释放只允许删除由自己 Owner Token 持有的锁；任何过期、迟到或身份不符的释放请求必须被拒绝。
_Avoid_: 安全释放、防误删锁

**Fencing Token（栅栏令牌）**:
每次成功获取事件签发的单调递增序号。下游存储用它拒绝旧持有者的迟到写入，是自动释放场景下唯一系统性的正确性补救。
_Avoid_: 版本号、epoch

### 操作语义

**可重入（Reentrancy）**:
同一 Owner Token 再次获取同一 Key 时，递增持有计数而非阻塞。计数保存在后端，不随句柄重建丢失。崩溃恢复场景（业务用 WAL 里的
token 重新获取）也走这条路径：崩溃前残留的计数由租约到期清除。
_Avoid_: 递归锁

**降级（Downgrade）**:
持有写锁期间原子地转换为读持有，期间不暴露无锁窗口。
_Avoid_: 锁转换、升级（升级是反方向，明确不做）

**多锁（MultiLock）**:
对一组 Key 的全有或全无获取：全部到手才成功，任一失败即回滚已获取的。获取按 Key 字典序进行以避免交叉死锁。v1 仅互斥模式。
_Avoid_: 锁组、批量锁
